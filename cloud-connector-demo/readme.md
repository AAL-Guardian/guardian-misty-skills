# Misty Connector Demo

This is a sample package to connect to Guardian MQTT Broker and listen for events

## Usage

- install node.js 12+
- run `npm i` to install packages
- run `npx tsc` to compile the typescript
- run `node ./dist/misty-connect.js` to make it run
